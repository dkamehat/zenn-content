# zenn-content

[Zenn](https://zenn.dev) articles, managed via GitHub deploy integration.

- `articles/` — one Markdown file per article (the filename is the slug).

Edit a file and push to `main`; Zenn syncs automatically.
